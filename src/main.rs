fn main() {
#[derive(Debug, Default, Clone, PartialEq, Eq)]
pub struct Mail {
    pub from: String,
    pub to: Vec<String>,
    pub data: String,
}

#[tokio::main]
async fn main() {
    println!("Hello, world!");
}
